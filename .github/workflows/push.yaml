name: push
on:
  workflow_dispatch:
  push:
    branches:
      - "main"
  pull:
    branches:
      - "main"
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      name: hello
      run: echo "Hello"
